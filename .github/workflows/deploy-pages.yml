name: Deploy simulator to GitHub Pages

# Builds the client (static) and publishes it to GitHub Pages on every push to
# main. The simulator works standalone with sample data; the "load real race"
# feature calls the public, read-only single_pick_ai API through Cloudflare.
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --no-frozen-lockfile
      - name: Build (base path = repo name for project Pages)
        env:
          VITE_SINGLE_PICK_AI_BASE: https://api.keibalab.net
        run: pnpm exec vite build --base=/${{ github.event.repository.name }}/
      - name: Add SPA fallback for direct route requests
        run: cp dist/public/index.html dist/public/404.html
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist/public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
