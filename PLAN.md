# KEIBA LAB レースビュー実装計画

## 視覚目標

札幌記念の16頭を、楕円形の芝コース上で走る馬番付きマーカーとして表示する。レース進捗・隊列・上位5頭・区間名を同一ビューに集約し、既存のシミュレーション開始、再生・一時停止、リプレイ操作と連動させる。

## リスクと検証基準

主要リスクは、16頭表示時の重なり、順位入替時の視認性、モバイル幅での操作性である。SVGの楕円経路、馬番、勝負服色、レーン分散で対応する。進捗が上がると各馬がコース上を移動し、上位表示と区間ラベルが更新されることを画面で検証する。
