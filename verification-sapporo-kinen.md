# 札幌記念データ反映の確認メモ

Managementの「札幌記念を読み込む」を実行した。2026年8月16日・札幌記念（GII）の出走馬16頭が反映され、距離は芝2,000m、天候は曇、馬場は良、想定ペースは平均として表示された。

反映後のシミュレーション結果では、シェイクユアハートが勝率37.5%で1位となり、表示件数6件のページネーションは全3ページへ更新された。Managementには、読み込み完了メッセージと馬番1から16までの実在馬名が表示されることを確認した。

メインステージの表示も第62回札幌記念・札幌11R・芝2,000m・曇／良／平均ペースへ同期された。コースリプレイを開始すると、16頭の馬番・勝負服カラーマーカーが楕円芝コース上で移動し、64%時点では向正面、100%時点では直線／FINISHEDとなった。上位5頭の順位表示とCSV出力可能な通過ログも同時に更新された。

実走フレーム化後は、能力値・脚質・シミュレーションスコアを入力にした決定論的な単一レースフレームから位置、順位、区間ログを生成する。札幌記念では40%時点でサクラファレル、アドマイヤテラ、イガッチ、ショウヘイ、エコロヴァルツの順に第1コーナーを通過し、同じレース実走データがコース表示・順位表・CSVログへ使用されることを確認した。

追従カメラの選択肢からシェイクユアハート（馬番8）を選ぶと、コース上の対象馬がハイライトされ、追従中の馬名が表示された。馬番マーカーには無地、縦縞、横縞、星、ダイヤの勝負服パターンが表示される。

追従中の状態でレースを完走し、HOME STRAIGHT／進捗100%の後にOFFICIAL RESULT「着順・タイム確定」が表示されることを確認した。確定演出には上位8頭の勝負服マーカー、順位、馬名、タイムが段階表示され、同一実走フレームの最終順位と一致する。

追従カメラでシェイクユアハートを選択すると、対象馬名と現在のズーム倍率1.12xが明示され、ズーム用スライダーが有効になることを確認した。

ズームスライダーを操作すると、追従中の倍率表示が1.35xへ更新され、コース表示も追従対象を中心に拡大された。完走後には「写真判定スロー」ボタンが表示され、ゴール直前の実走フレームから再生できる入口を確認した。

「写真判定スロー」を実行すると、進捗88%の最終コーナーから再開され、PHOTO FINISH · SLOW MOTIONバッジが表示された。ManagementのSILK STUDIOには保存元の選択、プリセット名、保存ボタン、各馬への適用選択が表示されることを確認した。

写真判定速度の選択肢として0.5×、0.75×、1.0×、1.5×、2.0×が表示され、2.0×を選択すると操作表示が2.0×へ更新されることを確認した。

札幌記念を完走すると、確定結果に勝ち馬の「—」に続き、シェイクユアハートの「ハナ」、ピンクジンの「½馬身」、イガッチとアドマイヤテラの「1馬身」など、実走フレームの最終位置から換算した着差がタイムと併記されることを確認した。

確定結果の着差基準には「前走馬との差」と「1着馬から」の選択肢が表示され、累積着差を選択した状態へ切り替わることを確認した。

累積着差で完走すると、確定結果の見出しが「1着馬から」と表示され、確定結果CSVボタンと写真判定スローボタンが表示されることを確認した。

写真判定スローを開始するとFRAME 88%と前後フレーム操作が表示される。ゴール後の前フレーム操作によりFRAME 96%へ戻り、手動コマ送り時はPAUSED状態となることを確認した。

次フレーム操作でFRAME 100%へ戻れることを確認し、「確定結果CSV」ボタンを実行すると「確定結果CSVを出力しました。」という成功通知が表示された。

使い方タブには、条件・出走馬設定、コース走行、写真判定、CSV出力の4ステップと、Managementのキーボード操作・写真判定の長押し連続送りに関するヒントが表示されることを確認した。

完走後の確定結果では、ハナ・クビ・½馬身などの競馬表記に加え、0.06馬身・0.31馬身などの換算値が併記された。RESULT CSV STUDIOには14種類の列選択とテンプレート名の入力、設定保存、選択列でのCSV出力操作が表示されることを確認した。

「写真判定・全列」という名前でCSV設定を保存すると、成功通知とともに14項目のテンプレートが表示され、適用・削除操作を利用できることを確認した。

写真判定スローのゴール前では、前後フレームの操作ボタンに「長押しで連続」操作であることが表示され、長押し開始後の連続送り制御が有効になることを確認した。
