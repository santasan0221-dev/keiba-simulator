# 保存・出力のprovenance検証記録

2026年8月15日に、single_pick_ai由来の検証データでCSVとPDFを生成した。CSVの先頭には、実データ元、`race_key`、`venue`、校正状態、`as_of`、取得時刻、既存のwhat-if免責文言を記録した。PDFはアプリで使用するNoto Sans JPを埋め込み、同じ項目と免責文言が抽出テキストでも確認できた。

検証データの校正状態は `UNCALIBRATED_SHADOW_SCORE` である。出力には校正済み確率を新たに作成・補間・代替表示していない。実レース未読込の保存では、同じ出所欄を「サンプルデータ（実レース未読込）」として出力する。旧形式のSnapshotに出所がない場合は「未確認（旧保存形式）」と扱う。
